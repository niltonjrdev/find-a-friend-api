services:
  db:
    image: postgres
    container_name: api-findafriend-pg
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: findafriend
    ports:
      - "5432:5432"
